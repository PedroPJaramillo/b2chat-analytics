# ==============================================================================
# ENVIRONMENT CONFIGURATION GUIDE
# ==============================================================================
#
# This file documents all available environment variables.
#
# ENVIRONMENT FILE LOADING ORDER (Next.js):
# 1. .env.local (highest priority, gitignored - for local overrides)
# 2. .env.development / .env.production (based on NODE_ENV)
# 3. .env (lowest priority, committed - for defaults)
#
# LOCAL DEVELOPMENT SETUP:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Update DATABASE_URL to point to local PostgreSQL (see examples below)
# 3. Fill in required API keys (Clerk, B2Chat, etc.)
# 4. Run: npm run db:local:setup
#
# PRODUCTION SETUP:
# Configure these variables in your deployment platform (Vercel, etc.)
# ==============================================================================

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------

# OPTION 1: Local PostgreSQL (recommended for development)
# Create database: npm run db:local:setup
# Then use these connection strings in .env.local (replace 'your_username' with your OS username):
DATABASE_URL="postgresql://your_username@localhost:5432/b2chat_analytics_dev"
POSTGRES_URL_NON_POOLING="postgresql://your_username@localhost:5432/b2chat_analytics_dev"
POSTGRES_PRISMA_URL="postgresql://your_username@localhost:5432/b2chat_analytics_dev"

# OPTION 2: Supabase (production/staging)
# Get these from your Supabase project settings > Database > Connection string
# DATABASE_URL="postgresql://postgres:[password]@[host]:6543/postgres?sslmode=require&pgbouncer=true"
# POSTGRES_URL_NON_POOLING="postgresql://postgres:[password]@[host]:5432/postgres?sslmode=require"
# POSTGRES_PRISMA_URL="postgresql://postgres:[password]@[host]:6543/postgres?sslmode=require&pgbouncer=true"

# ------------------------------------------------------------------------------
# Authentication (Clerk) - REQUIRED
# ------------------------------------------------------------------------------
# Get these from: https://dashboard.clerk.com/
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."
CLERK_WEBHOOK_SECRET="whsec_..."

# ------------------------------------------------------------------------------
# B2Chat API - REQUIRED
# ------------------------------------------------------------------------------
B2CHAT_API_URL="https://api.b2chat.io"
B2CHAT_USERNAME="your_username"
B2CHAT_PASSWORD="your_password"
B2CHAT_API_VERSION="v1"

# ------------------------------------------------------------------------------
# Claude API - REQUIRED for AI Analysis Features
# ------------------------------------------------------------------------------
CLAUDE_API_KEY="sk-ant-..."
CLAUDE_MODEL="claude-sonnet-4-5-20250929"

# ------------------------------------------------------------------------------
# File Storage - REQUIRED for Exports
# ------------------------------------------------------------------------------
# Get this from: https://vercel.com/dashboard > Storage > Blob
BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# ------------------------------------------------------------------------------
# Email Service - OPTIONAL
# ------------------------------------------------------------------------------
# Get this from: https://resend.com/api-keys
RESEND_API_KEY="re_..."
EMAIL_FROM="noreply@yourdomain.com"

# ------------------------------------------------------------------------------
# System Configuration
# ------------------------------------------------------------------------------
SYSTEM_TIMEZONE="America/Bogota"
DISPLAY_TIMEZONE="America/Bogota"
DEFAULT_PAGINATION_LIMIT=50
MAX_EXPORT_ROWS=10000

# ------------------------------------------------------------------------------
# Feature Flags
# ------------------------------------------------------------------------------
ENABLE_SCHEDULED_SYNC=true
ENABLE_ATTACHMENTS_BACKUP=true
ENABLE_NOTIFICATIONS=true
ENABLE_AI_ANALYSIS=true